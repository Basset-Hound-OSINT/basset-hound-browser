# v12.7.0 Extended Evasion Vectors - Detailed Feature Plan

**Document:** Detailed Feature Specification - Extended Evasion Vectors  
**Created:** June 14, 2026  
**Feature:** Extended Evasion Vectors (TLS + HTTP/2 + Timing + Advanced Detection)  
**Duration:** 3-4 days (12-16 hours)  
**Tests:** 80+ (25 TLS + 20 HTTP/2 + 15 Timing + 10 Network + 10 Detection Service)  
**LOC:** 1200-1600 lines of new code  
**Status:** READY FOR IMPLEMENTATION  

---

## EXECUTIVE SUMMARY

This feature plan details the implementation of 10+ advanced evasion vectors targeting:
- **TLS Fingerprinting** (JA3/JA4 bypass, cipher manipulation)
- **HTTP/2 Behavior** (header ordering, priority manipulation)
- **Timing Attacks** (request/response timing randomization)
- **Advanced Detection Services** (PerimeterX, DataDome, reCAPTCHA, Cloudflare, Distil)

**Goal:** Increase evasion effectiveness from 85-90% to 95%+ by implementing sophisticated network-layer evasion.

**Integration:** Plugs into existing multi-layer coordinator; 6 new WebSocket commands; zero breaking changes.

---

## CURRENT STATE ANALYSIS

### Existing Evasion Framework (v12.6.0)

**Current Capabilities:**
- Layer 1: TLS/Network (basic JA4, HTTP/2 SETTINGS)
- Layer 2: Browser API (Canvas, WebGL, AudioContext, fonts)
- Layer 3: Behavioral (micro-timing, mouse, scroll)
- Layer 4: Session (cookies, headers)
- Layer 5: Device (fingerprinting coherence)
- Layer 6: Coherence Validation (cross-layer)

**Current Effectiveness:** 85.5% → Target: 92-98% with new vectors

**Gap Analysis:**
| Layer | Current | Gap | v12.7.0 Target |
|-------|---------|-----|-----------------|
| TLS/Network | 70% | +25% | 95%+ |
| HTTP/2 | 65% | +20% | 85%+ |
| Timing | 75% | +15% | 90%+ |
| Detection Services | 60% | +30% | 90%+ |
| Overall Coherence | 85.5% | +10% | 95%+ |

### Existing Test Infrastructure

**Test Files:**
- `tests/evasion/advanced-evasion-comprehensive.test.js` (TLS, behavioral)
- `tests/evasion/advanced-evasion-vectors.test.js` (multiple vectors)
- `tests/evasion/detection-evasion-v2.test.js` (detection services)
- `tests/features/bot-detection-evasion-*.test.js` (real-world sites)

**Test Pattern:**
```javascript
describe('Feature Name', () => {
  let instance;
  beforeEach(() => {
    instance = new Module({ profile: 'chrome131-windows' });
  });
  
  test('should validate expected behavior', () => {
    const result = instance.method();
    expect(result).toBeDefined();
    expect(result.score).toBeGreaterThanOrEqual(threshold);
  });
});
```

### Existing WebSocket Integration Pattern

Current evasion commands in websocket/server.js:
```javascript
// Existing patterns - add new commands to mirror these
case 'get_evasion_status':
  // Returns { tls, http2, behavioral, session, device, coherence }
  
case 'test_evasion_effectiveness':
  // Tests against known signatures
```

---

## FEATURE 1: TLS FINGERPRINTING EVASION

**Duration:** 1.5 days | **Tests:** 25 | **LOC:** 400-500

### 1.1 Architecture

**File Structure:**
```
src/evasion/
  ├── tls-fingerprinting-evasion.js (EXISTING - extend)
  ├── tls-cipher-rotation.js (NEW)
  ├── tls-extension-ordering.js (NEW)
  ├── certificate-validation-evasion.js (NEW)
  ├── alpn-protocol-selection.js (NEW)
  └── tls-coherence-validator.js (extend existing)
```

### 1.2 Implementation Details

#### 1.2.1 Cipher Suite Rotation & Manipulation

**Objective:** Evade detection based on cipher suite ordering and selection patterns.

**Approach:**
```javascript
class TLSCipherRotation {
  constructor(profile = 'chrome131-windows') {
    this.profile = profile;
    this.baselineCiphers = this._loadProfileCiphers(profile);
    this.rotationIndex = 0;
    this.sessionCipherMap = new Map(); // Track per-session ciphers
  }

  // Rotate cipher suite with realistic variation
  getCipherSuite(sessionId, rotationStrategy = 'realistic') {
    // Strategy 1: "realistic" - rotate within Chrome-like sets (85% match)
    // Strategy 2: "aggressive" - wider rotation (70% match, higher risk)
    // Strategy 3: "conservative" - minimal variation (95% match, lower evasion)
    
    const ciphers = this._selectCiphers(rotationStrategy);
    const ordered = this._orderCiphersRealistically(ciphers);
    return this._validateAgainstJA4(ordered); // Ensure JA4 coherence
  }

  // TLS 1.2 Ciphers (32 total Chrome-like)
  _loadProfileCiphers(profile) {
    const TLS12_CIPHERS = [
      0x1301, // TLS_AES_128_GCM_SHA256
      0x1302, // TLS_AES_256_GCM_SHA384
      0x1303, // TLS_CHACHA20_POLY1305_SHA256
      // ... 29 more matching Chrome 131
    ];
    return TLS12_CIPHERS;
  }

  // Realistic cipher ordering based on browser preferences
  _orderCiphersRealistically(ciphers) {
    // Chrome prioritizes: AES-GCM > ChaCha20 > AES-CCM
    // Within each category: prefer stronger variants
    return ciphers.sort((a, b) => {
      const priorityA = this._getCipherPriority(a);
      const priorityB = this._getCipherPriority(b);
      return priorityB - priorityA;
    });
  }

  _getCipherPriority(cipherCode) {
    // 0x1301 (AES128-GCM) = 100 priority
    // 0x1302 (AES256-GCM) = 110 priority
    // 0x1303 (ChaCha20) = 95 priority
    // etc.
    const priorities = { /* cipher -> priority */ };
    return priorities[cipherCode] || 0;
  }
}
```

**Detection Methods Evaded:**
- JA3/JA4 fingerprinting (cipher order is key differentiator)
- TLS 1.2 ClientHello analysis
- Cipher suite frequency analysis
- Browser signature matching

**Test Coverage (8 tests):**
- [ ] Cipher suite rotation produces >100 unique permutations
- [ ] Rotated cipher suites pass JA3 validation
- [ ] Ordered ciphers match Chrome realistic preference order
- [ ] Aggressive rotation still maintains >70% Chrome-like patterns
- [ ] Conservative rotation achieves <2% variation from baseline
- [ ] Per-session cipher tracking is consistent across requests
- [ ] Cipher priority ordering never violates TLS constraints
- [ ] Rotation produces statistically distinct fingerprints across connections

#### 1.2.2 TLS Version Spoofing (1.2/1.3 Support)

**Objective:** Support TLS 1.2 and 1.3 with version-appropriate evasion.

**Approach:**
```javascript
class TLSVersionEvasion {
  constructor(allowedVersions = ['1.2', '1.3']) {
    this.allowedVersions = allowedVersions;
    this.currentVersion = '1.3'; // Chrome default
    this.versionCoherence = new Map(); // Track version per domain
  }

  // Select TLS version based on target and profile
  selectTLSVersion(targetDomain, profile = 'chrome131') {
    // Some older sites expect TLS 1.2
    // Most modern sites expect TLS 1.3
    // Return version + associated handshake parameters
    
    const version = this._selectVersionForDomain(targetDomain, profile);
    return {
      version,
      minVersion: version === '1.3' ? '1.2' : '1.0',
      handshakeParams: this._getHandshakeParams(version),
      supportedVersionsExtension: this._buildSupportedVersionsExt(),
      coherenceScore: this._validateVersionCoherence(version)
    };
  }

  _selectVersionForDomain(domain, profile) {
    // Check if domain has known version preference
    if (this.versionCoherence.has(domain)) {
      return this.versionCoherence.get(domain);
    }
    
    // Default: TLS 1.3 for 95% of sites, TLS 1.2 fallback
    const isCorporateOrLegacy = /bank|gov|corp/.test(domain);
    const version = isCorporateOrLegacy ? '1.2' : '1.3';
    
    this.versionCoherence.set(domain, version);
    return version;
  }

  _getHandshakeParams(version) {
    // TLS 1.3 parameters differ from 1.2
    if (version === '1.3') {
      return {
        keyShareGroups: ['x25519', 'x25519mlkem768', 'secp384r1'],
        supportedGroups: ['x25519', 'x25519mlkem768', 'secp384r1', 'secp256r1'],
        signatureAlgorithms: [
          0x0804, // rsa_pss_rsae_sha256
          0x0401, // ecdsa_secp256r1_sha256
          0x0503  // ecdsa_secp384r1_sha384
        ]
      };
    } else {
      return {
        signatureAlgorithms: [
          0x0401, // ecdsa_secp256r1_sha256
          0x0804, // rsa_pss_rsae_sha256
          0x0201  // rsa_pkcs1_sha256
        ],
        supportedGroups: ['secp256r1', 'secp384r1', 'ffdhe2048']
      };
    }
  }

  _buildSupportedVersionsExt() {
    // supported_versions extension (only in TLS 1.3)
    return ['0x0304', '0x0303']; // 1.3, 1.2
  }

  _validateVersionCoherence(version) {
    // Ensure selected version is compatible with cipher suites, groups, etc.
    // Score: 0-100 (higher = more consistent)
    return 95; // Example: 95% coherence
  }
}
```

**Detection Methods Evaded:**
- Server-side TLS version detection
- ClientHello handshake analysis
- Protocol downgrade attack detection (POODLE, etc.)
- Version availability logging

**Test Coverage (6 tests):**
- [ ] TLS 1.3 selection for modern sites
- [ ] TLS 1.2 fallback for legacy sites
- [ ] Per-domain version consistency maintained across requests
- [ ] Handshake parameters match selected TLS version
- [ ] supported_versions extension only present in TLS 1.3
- [ ] Version mismatch detection triggers fallback mechanism

#### 1.2.3 TLS Extension Ordering & ALPN Selection

**Objective:** Randomize extension ordering and ALPN protocol selection for evasion.

**Approach:**
```javascript
class TLSExtensionOrdering {
  constructor(profile = 'chrome131-windows') {
    this.profile = profile;
    this.baselineExtensions = this._loadExtensionsForProfile(profile);
    this.alpnProtocols = ['h2', 'http/1.1']; // HTTP/2 preferred
    this.extensionVariations = [];
  }

  // Get reordered extensions with realism constraints
  getExtensionOrder(reorderingStrategy = 'realistic') {
    // Strategy: 'realistic' = Chrome-like ordering (85% match)
    //           'aggressive' = More variation (65% match)
    //           'conservative' = Minimal variation (98% match)
    
    const extensions = this._selectExtensions(reorderingStrategy);
    const ordered = this._reorderRealistic(extensions, reorderingStrategy);
    return this._validateExtensionCoherence(ordered);
  }

  _loadExtensionsForProfile(profile) {
    // Chrome 131 extensions in default order
    const CHROME131_EXTENSIONS = [
      { id: 0x0000, name: 'server_name' },
      { id: 0x0001, name: 'max_fragment_length' },
      { id: 0x0005, name: 'status_request' },
      { id: 0x000a, name: 'supported_groups' },
      { id: 0x000b, name: 'ec_point_formats' },
      { id: 0x000d, name: 'signature_algorithms' },
      { id: 0x000f, name: 'padding' },
      { id: 0x0010, name: 'encrypt_then_mac' },
      { id: 0x0015, name: 'padding' }, // Intentional Chrome duplicate
      { id: 0x0016, name: 'session_ticket' },
      { id: 0x0023, name: 'session_ticket' },
      { id: 0x0028, name: 'key_share' },
      { id: 0x002b, name: 'supported_versions' },
      { id: 0x002d, name: 'psk_key_exchange_modes' },
      { id: 0x0033, name: 'key_share' },
      { id: 0x0034, name: 'post_quantum_key_share' }
    ];
    return CHROME131_EXTENSIONS;
  }

  // Reorder extensions while maintaining critical constraints
  _reorderRealistic(extensions, strategy) {
    if (strategy === 'conservative') {
      return extensions; // No reordering
    }

    // Critical ordering constraints:
    // 1. server_name (SNI) MUST be first
    // 2. padding extension MUST be present
    // 3. key_share must come before supported_versions
    // 4. psk_key_exchange_modes must come after session_ticket
    
    const reordered = [...extensions];
    
    if (strategy === 'realistic') {
      // 15% chance to reorder non-critical extensions
      const criticalIndices = [0, 1, 2]; // server_name, max_fragment_length, status_request
      const swapCount = Math.floor(Math.random() * 3); // 0-2 swaps
      
      for (let i = 0; i < swapCount; i++) {
        let idx1 = Math.floor(Math.random() * reordered.length);
        let idx2 = Math.floor(Math.random() * reordered.length);
        
        // Skip critical indices
        if (criticalIndices.includes(idx1) || criticalIndices.includes(idx2)) {
          continue;
        }
        
        [reordered[idx1], reordered[idx2]] = [reordered[idx2], reordered[idx1]];
      }
    } else if (strategy === 'aggressive') {
      // More aggressive reordering (35% variation)
      for (let i = 1; i < reordered.length; i++) { // Skip server_name
        const swapIdx = Math.floor(Math.random() * reordered.length);
        if (swapIdx !== 0) { // Never swap with server_name
          [reordered[i], reordered[swapIdx]] = [reordered[swapIdx], reordered[i]];
        }
      }
    }
    
    return this._enforceOrderingConstraints(reordered);
  }

  _enforceOrderingConstraints(extensions) {
    // Re-enforce critical ordering rules after reordering
    const constraints = [
      { name: 'server_name', position: 0 },
      // key_share before supported_versions constraint
    ];
    
    // Implementation enforces constraints
    return extensions;
  }

  // Select ALPN protocol with variation
  selectALPNProtocol(serverSupported = ['h2', 'http/1.1']) {
    // Realistic: h2 (HTTP/2) in 90% of cases, http/1.1 in 10%
    // Some old servers don't support h2
    
    const preferH2 = Math.random() < 0.95; // 95% prefer h2
    
    return {
      protocol: preferH2 ? 'h2' : 'http/1.1',
      supported: serverSupported,
      coherenceScore: this._validateALPNCoherence(preferH2)
    };
  }

  _validateExtensionCoherence(extensions) {
    // Ensure ordering is Chrome-like
    // Score 0-100 (higher = more Chrome-like)
    // Extensions in realistic order should score >85
    return extensions;
  }

  _validateALPNCoherence(preferH2) {
    // HTTP/2 selection should match TLS version and cipher suites
    // If TLS 1.2 and weak ciphers, prefer http/1.1
    return preferH2 ? 95 : 80;
  }
}
```

**Detection Methods Evaded:**
- Extension ordering fingerprinting
- ALPN protocol inference
- ClientHello uniqueness analysis
- Chrome version detection

**Test Coverage (6 tests):**
- [ ] Extension ordering produces >50 unique patterns
- [ ] server_name extension always first
- [ ] key_share comes before supported_versions
- [ ] Padding extension present in all handshakes
- [ ] ALPN selection matches statistical distribution
- [ ] Reordered extensions pass TLS coherence validation

#### 1.2.4 Certificate Validation Evasion

**Objective:** Handle certificate validation bypass techniques.

**Approach:**
```javascript
class CertificateValidationEvasion {
  constructor(profile = 'chrome131-windows') {
    this.profile = profile;
    this.certificateCache = new Map(); // Cache validated certs
    this.trustedCAs = this._loadSystemCACerts();
    this.pinningBypass = { enabled: false, domains: [] };
  }

  // Validate server certificate realistically
  validateServerCertificate(cert, domain) {
    // Real browsers verify:
    // 1. Certificate chain is valid
    // 2. Domain matches CN or SAN
    // 3. Certificate not expired
    // 4. Signature valid
    
    const validation = {
      valid: true,
      domain_match: this._validateDomainMatch(cert, domain),
      chain_valid: this._validateChain(cert),
      signature_valid: this._validateSignature(cert),
      not_expired: this._validateExpiration(cert),
      pinning_check: this._validatePinning(domain, cert),
      issues: []
    };

    // Log for evasion tracking
    this._logValidationEvent(domain, validation);
    
    return validation;
  }

  // Domain matching (handles wildcards, etc.)
  _validateDomainMatch(cert, domain) {
    const subjectAltName = cert.subjectAltName || [];
    const commonName = cert.subject?.CN || '';
    
    const allowed = [...subjectAltName, commonName];
    
    for (const pattern of allowed) {
      if (pattern.startsWith('*.')) {
        // Wildcard: *.example.com matches subdomain.example.com
        const regex = new RegExp('^' + pattern.replace('.', '\\.').replace('*', '[^.]+') + '$');
        if (regex.test(domain)) return true;
      } else if (pattern === domain) {
        return true;
      }
    }
    
    return false;
  }

  // Validate certificate chain
  _validateChain(cert) {
    // Each cert must be signed by the next cert in chain
    // Chain must end with trusted root CA
    // Implementation validates cryptographic signatures
    return true;
  }

  // Validate certificate signature
  _validateSignature(cert) {
    // Verify sig was created by issuer's private key
    // Check signature algorithm is secure
    return true;
  }

  // Check expiration date
  _validateExpiration(cert) {
    const now = new Date();
    return cert.validity.notAfter > now;
  }

  // Check for certificate pinning (HPKP, browser pins)
  _validatePinning(domain, cert) {
    // Some sites use certificate pinning (public key pinning)
    // Return: { pinned: boolean, bypass_available: boolean }
    
    const pinningRules = this._getPinningRulesForDomain(domain);
    if (!pinningRules) return { pinned: false };
    
    const publicKeyHash = this._hashPublicKey(cert);
    const pinned = pinningRules.hashes.includes(publicKeyHash);
    
    return {
      pinned,
      bypass_available: false, // Only bypass if we have backup certs
      known_bypasses: this._getKnownBypassesForDomain(domain)
    };
  }

  _logValidationEvent(domain, validation) {
    // Track certificate validation for debugging
    // Can help identify which sites have unusual requirements
  }

  _getPinningRulesForDomain(domain) {
    // Known pinning rules for major sites
    // Example: { domain: 'github.com', hashes: [...], backups: [...] }
    return null; // Not pinned by default
  }

  _getKnownBypassesForDomain(domain) {
    // Some sites have known bypass methods
    // Stored for future reference
    return [];
  }

  _hashPublicKey(cert) {
    // SHA256 hash of public key for pinning
    return ''; // Simplified
  }

  _loadSystemCACerts() {
    // Load system CA certificates
    return [];
  }
}
```

**Detection Methods Evaded:**
- Certificate pinning attacks
- MITM detection via certificate inspection
- Unusual certificate chain patterns
- Certificate transparency logging

**Test Coverage (5 tests):**
- [ ] Valid certificates pass validation
- [ ] Expired certificates rejected
- [ ] Domain mismatch detected
- [ ] Invalid chain rejected
- [ ] Pinning rules enforced

### 1.3 Integration with Multi-Layer Coordinator

**Integration Point:**
```javascript
// In multi-layer-coordinator.js, TLS layer:
async initializeTLSLayer() {
  const tlsConfig = {
    cipherRotation: new TLSCipherRotation(this.profile),
    versionEvasion: new TLSVersionEvasion(),
    extensionOrdering: new TLSExtensionOrdering(this.profile),
    certificateValidation: new CertificateValidationEvasion(this.profile)
  };
  
  this.layers.tls = tlsConfig;
  return this.validateTLSCoherence();
}

validateTLSCoherence() {
  // Ensure all TLS components are coherent:
  // 1. Cipher suites valid for selected TLS version
  // 2. Extensions compatible with version
  // 3. ALPN protocols match TLS version expectations
  // 4. Certificate validation matches browser expectations
  
  const score = this._calculateTLSCoherenceScore();
  return { 
    coherent: score > 85, 
    score, 
    issues: this._detectTLSIssues() 
  };
}
```

### 1.4 Effectiveness Metrics

**Expected Improvements:**
- JA3 fingerprinting evasion: 70% → 90%+
- TLS ClientHello uniqueness: 60 → 200+ variations
- Cipher suite variability: 5 → 15+ permutations per profile
- Detection service bypass: 65% → 88%+

**Real-World Test Sites (Target Evasion):**
- TLS fingerprinting detectors (tls.tools)
- JA3 detection services (shodan, TLDR)
- Cipher suite analysis (ssllabs)
- Certificate inspection services

---

## FEATURE 2: HTTP/2 EVASION

**Duration:** 1.5 days | **Tests:** 20 | **LOC:** 300-400

### 2.1 Architecture

**File Structure:**
```
src/evasion/
  ├── http2-evasion.js (NEW)
  ├── http2-header-ordering.js (NEW)
  ├── http2-priority-manipulation.js (NEW)
  ├── http2-window-obfuscation.js (NEW)
  └── http2-settings-evasion.js (extend existing)
```

### 2.2 Implementation Details

#### 2.2.1 Header Frame Ordering & Pseudo-Header Reordering

**Objective:** Randomize HTTP/2 header ordering to evade fingerprinting.

**Approach:**
```javascript
class HTTP2HeaderOrdering {
  constructor(profile = 'chrome131-windows') {
    this.profile = profile;
    this.baselineHeaders = this._loadProfileHeaders(profile);
    this.pseudoHeaderOrder = this._getPseudoHeaderOrder(profile);
  }

  // Get reordered headers with realistic constraints
  getHeaderOrder(headers, reorderStrategy = 'realistic') {
    // HTTP/2 requires pseudo-headers (starting with ':') before regular headers
    // Within pseudo-headers, order can vary
    
    const pseudoHeaders = headers.filter(h => h.name.startsWith(':'));
    const regularHeaders = headers.filter(h => !h.name.startsWith(':'));
    
    const orderedPseudo = this._reorderPseudoHeaders(pseudoHeaders, reorderStrategy);
    const orderedRegular = this._reorderRegularHeaders(regularHeaders, reorderStrategy);
    
    return [...orderedPseudo, ...orderedRegular];
  }

  _loadProfileHeaders(profile) {
    // Chrome 131 default header order
    return {
      pseudo: [':authority', ':method', ':scheme', ':path'],
      regular: ['user-agent', 'accept', 'accept-language', 'accept-encoding', ...]
    };
  }

  _getPseudoHeaderOrder(profile) {
    // RFC 9113 requires specific pseudo-header ordering
    // :authority (or Host for HTTP/1.1)
    // :method
    // :scheme
    // :path
    
    // Chrome allows limited variation
    return [':authority', ':method', ':scheme', ':path'];
  }

  _reorderPseudoHeaders(pseudoHeaders, strategy) {
    // Pseudo-headers have stricter ordering rules
    // ':authority' SHOULD be first (but can be anywhere before data)
    // ':path' SHOULD be last
    
    if (strategy === 'conservative') {
      return pseudoHeaders.sort((a, b) => {
        const order = [':authority', ':method', ':scheme', ':path'];
        return order.indexOf(a.name) - order.indexOf(b.name);
      });
    }
    
    // Realistic: 10% chance of variation
    if (Math.random() < 0.10) {
      const reordered = [...pseudoHeaders];
      // Swap non-critical pseudo-headers
      if (reordered.length > 2) {
        const idx1 = 1 + Math.floor(Math.random() * (reordered.length - 2));
        const idx2 = 1 + Math.floor(Math.random() * (reordered.length - 2));
        [reordered[idx1], reordered[idx2]] = [reordered[idx2], reordered[idx1]];
      }
      return reordered;
    }
    
    return pseudoHeaders;
  }

  _reorderRegularHeaders(regularHeaders, strategy) {
    if (strategy === 'conservative') {
      return regularHeaders; // No reordering
    }
    
    // Realistic: randomize 20% of header order
    const reordered = [...regularHeaders];
    
    const shuffleCount = Math.ceil(reordered.length * 0.2);
    for (let i = 0; i < shuffleCount; i++) {
      const idx1 = Math.floor(Math.random() * reordered.length);
      const idx2 = Math.floor(Math.random() * reordered.length);
      [reordered[idx1], reordered[idx2]] = [reordered[idx2], reordered[idx1]];
    }
    
    return this._enforceHeaderConstraints(reordered);
  }

  _enforceHeaderConstraints(headers) {
    // Some headers have ordering preferences:
    // - user-agent should come before accept
    // - accept should come before accept-language
    // - etc.
    
    return headers;
  }
}
```

**Detection Methods Evaded:**
- HTTP/2 header ordering fingerprinting
- Pseudo-header position analysis
- Header uniqueness fingerprinting
- Browser HTTP/2 behavior profiling

**Test Coverage (5 tests):**
- [ ] Pseudo-headers come before regular headers
- [ ] Pseudo-headers maintain valid RFC ordering
- [ ] Regular header reordering produces >50 variations
- [ ] Reordered headers still satisfy HTTP/2 constraints
- [ ] Header order randomization is consistent per profile

#### 2.2.2 HTTP/2 Priority Manipulation

**Objective:** Randomize stream priority settings for evasion.

**Approach:**
```javascript
class HTTP2PriorityManipulation {
  constructor(profile = 'chrome131-windows') {
    this.profile = profile;
    this.defaultPriorities = this._loadDefaultPriorities(profile);
    this.streamPriorities = new Map(); // stream_id -> priority_info
  }

  // Set stream priority with evasion
  setStreamPriority(streamId, weight = 16, depends_on = 0, exclusive = false, evasionLevel = 'realistic') {
    // HTTP/2 priority: weight (1-256), dependency, exclusive bit
    // Browsers use heuristics:
    // - Initial requests: high priority
    // - Images/resources: lower priority
    // - Same-origin scripts: high priority
    
    const priority = this._calculatePriority(streamId, weight, evasionLevel);
    
    this.streamPriorities.set(streamId, {
      weight: priority.weight,
      depends_on: priority.depends_on,
      exclusive: priority.exclusive,
      timestamp: Date.now(),
      evasionLevel
    });
    
    return priority;
  }

  _calculatePriority(streamId, baseWeight, evasionLevel) {
    if (evasionLevel === 'conservative') {
      // Use default browser priorities
      return {
        weight: baseWeight,
        depends_on: 0,
        exclusive: false
      };
    }
    
    // Realistic: Add randomization to weight (±10%)
    const variance = baseWeight * 0.1 * (Math.random() - 0.5);
    const weight = Math.max(1, Math.min(256, Math.round(baseWeight + variance)));
    
    // 20% chance to depend on another stream
    const dependsOnOther = Math.random() < 0.20;
    const depends_on = dependsOnOther ? this._selectDependentStream() : 0;
    
    // 5% chance to set exclusive bit
    const exclusive = Math.random() < 0.05;
    
    return { weight, depends_on, exclusive };
  }

  _selectDependentStream() {
    // Select a random active stream to depend on
    const activeStreams = Array.from(this.streamPriorities.keys());
    return activeStreams.length > 0 
      ? activeStreams[Math.floor(Math.random() * activeStreams.length)]
      : 0;
  }

  _loadDefaultPriorities(profile) {
    // Chrome defaults for different resource types
    return {
      html: { weight: 255, depends_on: 0, exclusive: false },
      css: { weight: 220, depends_on: 0, exclusive: false },
      script: { weight: 200, depends_on: 0, exclusive: false },
      image: { weight: 100, depends_on: 0, exclusive: false },
      xhr: { weight: 180, depends_on: 0, exclusive: false },
      other: { weight: 128, depends_on: 0, exclusive: false }
    };
  }

  // Update priority mid-stream (Chrome does this)
  updateStreamPriority(streamId, newWeight, evasionLevel = 'realistic') {
    // Browsers can update priority as they learn resource importance
    // 5-10% of streams get priority updates
    
    if (!this.streamPriorities.has(streamId)) {
      return null;
    }
    
    const current = this.streamPriorities.get(streamId);
    const updated = this._calculatePriority(streamId, newWeight, evasionLevel);
    
    this.streamPriorities.set(streamId, {
      ...current,
      ...updated,
      updated_at: Date.now()
    });
    
    return updated;
  }

  // Get priority statistics for coherence validation
  getPriorityStatistics() {
    const priorities = Array.from(this.streamPriorities.values());
    
    return {
      average_weight: priorities.reduce((sum, p) => sum + p.weight, 0) / priorities.length,
      weight_variance: this._calculateVariance(priorities.map(p => p.weight)),
      streams_with_dependencies: priorities.filter(p => p.depends_on !== 0).length,
      exclusive_streams: priorities.filter(p => p.exclusive).length,
      total_streams: priorities.length,
      coherence_score: this._validatePriorityCoherence(priorities)
    };
  }

  _calculateVariance(values) {
    const mean = values.reduce((a, b) => a + b) / values.length;
    const variance = values.reduce((a, b) => a + Math.pow(b - mean, 2), 0) / values.length;
    return Math.sqrt(variance);
  }

  _validatePriorityCoherence(priorities) {
    // Ensure priorities are realistic
    // Score: 0-100 (higher = more realistic)
    
    // Check: most streams have weight between 50-255
    const reasonable = priorities.filter(p => p.weight >= 50 && p.weight <= 255).length;
    const reasonableScore = (reasonable / priorities.length) * 50;
    
    // Check: dependency tree is acyclic
    const acyclic = this._validateNoCycles();
    const acyclicScore = acyclic ? 30 : 0;
    
    // Check: exclusive bit usage is limited
    const exclusiveCount = priorities.filter(p => p.exclusive).length;
    const exclusiveScore = (1 - exclusiveCount / priorities.length) * 20;
    
    return reasonableScore + acyclicScore + exclusiveScore;
  }

  _validateNoCycles() {
    // Ensure priority dependency tree has no cycles
    return true; // Simplified
  }
}
```

**Detection Methods Evaded:**
- HTTP/2 priority fingerprinting
- Resource loading order inference
- Browser priority heuristic matching
- Stream dependency tree analysis

**Test Coverage (5 tests):**
- [ ] Priority weights vary realistically (±10% of baseline)
- [ ] Weight values stay within valid range (1-256)
- [ ] 20% of streams depend on other streams
- [ ] Exclusive bit set on <5% of streams
- [ ] Priority dependency tree is acyclic

#### 2.2.3 Window Size Obfuscation & Flow Control

**Objective:** Randomize HTTP/2 flow control window sizes.

**Approach:**
```javascript
class HTTP2WindowObfuscation {
  constructor(profile = 'chrome131-windows') {
    this.profile = profile;
    this.connectionWindow = 65535; // Initial connection window
    this.streamWindows = new Map(); // stream_id -> window_size
    this.windowUpdateStrategy = 'realistic';
  }

  // Set initial window size with variation
  setInitialWindowSize(windowSize = 65535, evasionLevel = 'realistic') {
    // RFC 7540: initial window size is 2^16-1 (65535)
    // Some browsers use different values
    
    if (evasionLevel === 'conservative') {
      this.connectionWindow = 65535; // Standard
    } else {
      // Realistic: 90% standard, 10% variation (±5%)
      if (Math.random() < 0.90) {
        this.connectionWindow = 65535;
      } else {
        const variance = 65535 * 0.05 * (Math.random() - 0.5);
        this.connectionWindow = Math.round(65535 + variance);
      }
    }
    
    return this.connectionWindow;
  }

  // Send SETTINGS frame with variation
  getSETTINGSFrame(evasionLevel = 'realistic') {
    // HTTP/2 SETTINGS frame contains various parameters
    // Different browsers use different values
    
    const settings = {
      HEADER_TABLE_SIZE: this._getHeaderTableSize(evasionLevel),
      ENABLE_PUSH: Math.random() < 0.95 ? 1 : 0, // Most keep it enabled
      MAX_CONCURRENT_STREAMS: this._getMaxConcurrentStreams(evasionLevel),
      INITIAL_WINDOW_SIZE: this._getInitialWindowSize(evasionLevel),
      MAX_FRAME_SIZE: this._getMaxFrameSize(evasionLevel),
      MAX_HEADER_LIST_SIZE: this._getMaxHeaderListSize(evasionLevel)
    };
    
    return {
      frame_type: 0x04, // SETTINGS frame
      flags: 0x00,
      stream_id: 0x00,
      settings: settings,
      coherence_score: this._validateSETTINGSCoherence(settings)
    };
  }

  _getHeaderTableSize(evasionLevel) {
    // Default: 4096 bytes
    if (evasionLevel === 'conservative') return 4096;
    
    // Realistic: 95% default, 5% variation
    if (Math.random() < 0.95) return 4096;
    
    const values = [2048, 4096, 8192];
    return values[Math.floor(Math.random() * values.length)];
  }

  _getMaxConcurrentStreams(evasionLevel) {
    // Most browsers: unlimited (not sent)
    // Some older browsers: 100
    if (evasionLevel === 'conservative') return null; // Not sent
    
    // Realistic: 90% unlimited, 10% limited
    if (Math.random() < 0.90) return null;
    
    const values = [50, 100, 128, 200];
    return values[Math.floor(Math.random() * values.length)];
  }

  _getInitialWindowSize(evasionLevel) {
    // Standard: 65535
    if (evasionLevel === 'conservative') return 65535;
    
    // Realistic: mostly standard, 10% variation
    if (Math.random() < 0.90) return 65535;
    
    const variance = 65535 * 0.1 * (Math.random() - 0.5);
    return Math.max(1, Math.min(2147483647, Math.round(65535 + variance)));
  }

  _getMaxFrameSize(evasionLevel) {
    // Standard: 16384 bytes
    // Range: 16384-16777215
    if (evasionLevel === 'conservative') return 16384;
    
    // Realistic: 80% standard, 20% variation
    if (Math.random() < 0.80) return 16384;
    
    const values = [16384, 32768, 65536];
    return values[Math.floor(Math.random() * values.length)];
  }

  _getMaxHeaderListSize(evasionLevel) {
    // Default: not set (unlimited)
    // Some browsers: set to large value for robustness
    if (evasionLevel === 'conservative') return null;
    
    // Realistic: 85% not set, 15% set to large value
    if (Math.random() < 0.85) return null;
    
    return 16384 * Math.floor(Math.random() * 4 + 2); // 32KB-64KB
  }

  // Handle window update frames (flow control)
  getWindowUpdateFrame(streamId, bytesConsumed, evasionLevel = 'realistic') {
    // Browser sends WINDOW_UPDATE after consuming data
    // Different browsers update at different thresholds
    
    const windowSize = this.streamWindows.get(streamId) || 65535;
    const remaining = windowSize - bytesConsumed;
    
    // Chrome: updates when window drops below 50% of initial
    const updateThreshold = 65535 * 0.5;
    
    if (remaining < updateThreshold) {
      const incrementSize = this._getWindowIncrementSize(windowSize, evasionLevel);
      
      return {
        frame_type: 0x08, // WINDOW_UPDATE frame
        stream_id: streamId,
        window_increment: incrementSize,
        coherence_score: this._validateWindowUpdate(streamId, incrementSize)
      };
    }
    
    return null; // No update needed
  }

  _getWindowIncrementSize(currentWindow, evasionLevel) {
    // Restore window to near-maximum
    const targetWindow = 65535;
    const baseIncrement = targetWindow - currentWindow;
    
    if (evasionLevel === 'conservative') {
      return baseIncrement;
    }
    
    // Realistic: ±5% variation
    const variance = baseIncrement * 0.05 * (Math.random() - 0.5);
    return Math.max(1, Math.round(baseIncrement + variance));
  }

  _validateSETTINGSCoherence(settings) {
    // Ensure SETTINGS values are consistent
    // Score: 0-100 (higher = more realistic)
    
    let score = 100;
    
    // Deduct for unusual combinations
    if (settings.MAX_CONCURRENT_STREAMS && settings.MAX_CONCURRENT_STREAMS < 50) {
      score -= 10; // Suspiciously low
    }
    
    if (settings.INITIAL_WINDOW_SIZE > 2147483647 || settings.INITIAL_WINDOW_SIZE < 1) {
      score -= 20; // Out of valid range
    }
    
    return Math.max(0, score);
  }

  _validateWindowUpdate(streamId, increment) {
    // Ensure window update is reasonable
    // Score: 0-100
    
    if (increment <= 0 || increment > 2147483647) {
      return 0; // Invalid
    }
    
    return 85 + Math.random() * 15; // 85-100
  }
}
```

**Detection Methods Evaded:**
- Flow control window size fingerprinting
- SETTINGS frame analysis
- Window update pattern detection
- Browser flow control heuristic matching

**Test Coverage (5 tests):**
- [ ] Initial window sizes vary realistically
- [ ] SETTINGS frame values are within valid ranges
- [ ] ENABLE_PUSH set in 95% of cases
- [ ] Window increments keep stream alive
- [ ] Window update timing is realistic

#### 2.2.4 Settings Frame Spoofing

**Objective:** Spoof HTTP/2 SETTINGS frame values to match different browsers.

**Approach:**
```javascript
class HTTP2SettingsEvasion {
  constructor(profile = 'chrome131-windows') {
    this.profile = profile;
    this.profileSettings = this._loadProfileSettings(profile);
    this.settingsFramesSent = 0;
  }

  // Generate SETTINGS frame that matches profile
  generateSETTINGSFrame(useProfileSettings = true) {
    if (useProfileSettings) {
      return this.profileSettings;
    }
    
    // Or generate variation
    return this._generateVariedSETTINGS();
  }

  _loadProfileSettings(profile) {
    // Load HTTP/2 SETTINGS for different browser profiles
    const profiles = {
      'chrome131-windows': {
        HEADER_TABLE_SIZE: 65536,
        ENABLE_PUSH: 1,
        MAX_CONCURRENT_STREAMS: null, // Not sent
        INITIAL_WINDOW_SIZE: 65535,
        MAX_FRAME_SIZE: 16384,
        MAX_HEADER_LIST_SIZE: null
      },
      'firefox121-windows': {
        HEADER_TABLE_SIZE: 4096,
        ENABLE_PUSH: 1,
        MAX_CONCURRENT_STREAMS: null,
        INITIAL_WINDOW_SIZE: 65535,
        MAX_FRAME_SIZE: 16384,
        MAX_HEADER_LIST_SIZE: 32768
      },
      'safari17-macos': {
        HEADER_TABLE_SIZE: 4096,
        ENABLE_PUSH: 1,
        MAX_CONCURRENT_STREAMS: null,
        INITIAL_WINDOW_SIZE: 65535,
        MAX_FRAME_SIZE: 16384,
        MAX_HEADER_LIST_SIZE: 8192
      }
    };
    
    return profiles[profile] || profiles['chrome131-windows'];
  }

  _generateVariedSETTINGS() {
    // Generate realistic variation from baseline
    const base = this.profileSettings;
    
    return {
      HEADER_TABLE_SIZE: base.HEADER_TABLE_SIZE + Math.floor((Math.random() - 0.5) * 4096),
      ENABLE_PUSH: base.ENABLE_PUSH,
      MAX_CONCURRENT_STREAMS: base.MAX_CONCURRENT_STREAMS,
      INITIAL_WINDOW_SIZE: base.INITIAL_WINDOW_SIZE,
      MAX_FRAME_SIZE: base.MAX_FRAME_SIZE,
      MAX_HEADER_LIST_SIZE: base.MAX_HEADER_LIST_SIZE ? base.MAX_HEADER_LIST_SIZE + Math.floor((Math.random() - 0.5) * 4096) : null
    };
  }

  // Validate SETTINGS across protocol upgrade
  validateSettingsConsistency() {
    // Check that we can:
    // 1. Re-apply same SETTINGS on reconnect
    // 2. Handle SETTINGS ACK
    // 3. Respect server SETTINGS changes
    
    return {
      consistent: true,
      reapplies_on_reconnect: true,
      handles_server_settings: true,
      coherence_score: 95
    };
  }
}
```

**Detection Methods Evaded:**
- Browser SETTINGS fingerprinting
- HTTP/2 protocol parameter analysis
- Browser version detection via SETTINGS
- Settings immutability checking

**Test Coverage (5 tests):**
- [ ] SETTINGS frame matches selected profile
- [ ] SETTINGS values are within valid ranges
- [ ] Profile-specific settings are preserved
- [ ] Variation stays within realistic bounds
- [ ] SETTINGS are re-applied on reconnection

### 2.3 Integration with Multi-Layer Coordinator

**Integration Point:**
```javascript
// In multi-layer-coordinator.js, HTTP/2 layer:
async initializeHTTP2Layer() {
  const http2Config = {
    headerOrdering: new HTTP2HeaderOrdering(this.profile),
    priorityManipulation: new HTTP2PriorityManipulation(this.profile),
    windowObfuscation: new HTTP2WindowObfuscation(this.profile),
    settingsEvasion: new HTTP2SettingsEvasion(this.profile)
  };
  
  this.layers.http2 = http2Config;
  return this.validateHTTP2Coherence();
}

validateHTTP2Coherence() {
  // Ensure all HTTP/2 components work together:
  // 1. Headers follow RFC 9113 rules
  // 2. Priorities form valid dependency tree
  // 3. Flow control window sizes are reasonable
  // 4. SETTINGS are consistent with TLS
  
  const score = this._calculateHTTP2CoherenceScore();
  return { 
    coherent: score > 85, 
    score, 
    issues: this._detectHTTP2Issues() 
  };
}
```

### 2.4 Effectiveness Metrics

**Expected Improvements:**
- HTTP/2 header fingerprinting evasion: 65% → 85%+
- Priority manipulation variations: 10 → 50+
- SETTINGS frame variability: 3 → 15+ configurations
- Detection service bypass: 60% → 82%+

---

## FEATURE 3: TIMING ATTACK PREVENTION

**Duration:** 0.75 days | **Tests:** 15 | **LOC:** 200-300

### 3.1 Architecture

**File Structure:**
```
src/evasion/
  ├── timing-attack-prevention.js (NEW)
  ├── request-timing-randomization.js (NEW)
  ├── response-delay-injection.js (NEW)
  ├── connection-reuse-patterns.js (NEW)
  └── timing-validator.js (NEW)
```

### 3.2 Implementation Details

#### 3.2.1 Request Timing Randomization

```javascript
class RequestTimingRandomization {
  constructor() {
    this.lastRequestTime = 0;
    this.minDelay = 10; // ms
    this.maxDelay = 150; // ms
    this.burstThreshold = 5; // Requests before natural pause
    this.requestCount = 0;
  }

  // Get delay before sending next request
  getRequestDelay(requestType = 'normal') {
    // Human behavior: varying delays between requests
    // 10-50ms: same-page resources (fast)
    // 50-150ms: user navigation (normal)
    // 200-500ms: user-triggered action (deliberate)
    
    let baseDelay = this._getBaseDelay(requestType);
    let variance = this._addVariance(baseDelay);
    
    // Every 5-7 requests, add longer pause (thinking time)
    if (this.requestCount > this.burstThreshold) {
      variance += this._addThinkingTime();
      this.requestCount = 0;
    } else {
      this.requestCount++;
    }
    
    return Math.max(this.minDelay, variance);
  }

  _getBaseDelay(requestType) {
    const delays = {
      'resource': 15,      // Image/CSS/JS
      'xhr': 60,           // XMLHttpRequest
      'fetch': 70,         // Fetch API
      'navigation': 100,   // Page navigation
      'form': 150          // Form submission
    };
    
    return delays[requestType] || 60;
  }

  _addVariance(baseDelay) {
    // ±20% variance on base delay
    const variance = baseDelay * 0.2 * (Math.random() - 0.5);
    
    // Normal distribution approximation (triangular)
    const sum = Math.random() + Math.random() + Math.random();
    const normalized = (sum - 1.5) / 1.5;
    
    return baseDelay + normalized * variance;
  }

  _addThinkingTime() {
    // Occasional longer pauses (100-500ms) to simulate thinking
    return 100 + Math.random() * 400;
  }
}
```

#### 3.2.2 Response Delay Injection

```javascript
class ResponseDelayInjection {
  constructor() {
    this.processingTime = 5; // ms base processing time
    this.userDetectionDelay = 0;
  }

  // Inject realistic processing delay
  getProcessingDelay(responseSize, isUserInteractive = false) {
    // Larger responses take slightly longer to process
    // User-interactive responses have more variable timing
    
    const baseDelay = this._calculateProcessingTime(responseSize);
    const variance = this._addHumanVariance(isUserInteractive);
    
    return baseDelay + variance;
  }

  _calculateProcessingTime(responseSize) {
    // ~1ms per 100KB
    return Math.ceil(responseSize / 100000);
  }

  _addHumanVariance(isUserInteractive) {
    if (isUserInteractive) {
      // 5-50ms variance
      return 5 + Math.random() * 45;
    } else {
      // 1-15ms variance
      return 1 + Math.random() * 14;
    }
  }
}
```

#### 3.2.3 Connection Reuse Patterns

```javascript
class ConnectionReusePatterns {
  constructor() {
    this.connections = new Map(); // domain -> connection_info
    this.reuseThreshold = 0.85; // 85% of requests reuse connection
  }

  // Decide whether to reuse existing connection
  shouldReuseConnection(domain, isSecure = true) {
    // Humans usually reuse connections for same-origin resources
    // Occasional new connections (network errors, timeouts)
    
    if (!this.connections.has(domain)) {
      return false; // No existing connection
    }
    
    const conn = this.connections.get(domain);
    
    // 85% chance to reuse
    if (Math.random() < this.reuseThreshold) {
      return true;
    }
    
    // 15% chance: new connection (simulate network issue)
    conn.connectionCount += 1;
    return false;
  }

  recordConnection(domain, connectionId) {
    if (!this.connections.has(domain)) {
      this.connections.set(domain, {
        connectionCount: 1,
        firstSeen: Date.now(),
        lastUsed: Date.now(),
        reuseCount: 0
      });
    } else {
      const conn = this.connections.get(domain);
      conn.reuseCount += 1;
      conn.lastUsed = Date.now();
    }
  }
}
```

### 3.3 Test Coverage (15 tests)

- [ ] Request delays vary ±20% from baseline
- [ ] Thinking time added every 5-7 requests
- [ ] Response delay scales with response size
- [ ] User-interactive responses have higher variance
- [ ] Connection reuse rate 80-90%
- [ ] New connections created randomly
- [ ] Delay values are always positive
- [ ] Variance distribution is normal-like
- [ ] Timing pattern doesn't reveal automation
- [ ] Cross-origin requests have independent timing
- [ ] Form submissions have higher delays
- [ ] Resource loading follows realistic pattern
- [ ] Network timeouts simulated occasionally
- [ ] Delay coherence validated over 100+ requests
- [ ] Timing doesn't correlate with request content

---

## FEATURE 4: ADVANCED NETWORK EVASION

**Duration:** 0.75 days | **Tests:** 10 | **LOC:** 200-300

### 4.1 Implementation Details

#### 4.1.1 DNS Query Pattern Obfuscation

```javascript
class DNSPatternObfuscation {
  constructor() {
    this.dnsQueryCache = new Map();
    this.queryPattern = 'normal'; // normal, aggressive, paranoid
  }

  // Randomize DNS query pattern
  getDNSQueryPattern(domain) {
    // Normal: Cache lookups, reuse across session
    // Aggressive: New lookup per domain (unusual)
    // Paranoid: Multiple lookups, randomize timing
    
    if (this.queryPattern === 'normal') {
      if (this.dnsQueryCache.has(domain)) {
        return this.dnsQueryCache.get(domain);
      }
    }
    
    const result = this._performDNSLookup(domain);
    this.dnsQueryCache.set(domain, result);
    
    return result;
  }

  _performDNSLookup(domain) {
    // Vary DNS behavior based on pattern
    return {
      domain,
      delay: 5 + Math.random() * 50,
      resolved: '1.2.3.4',
      ttl: 300 + Math.random() * 900
    };
  }
}
```

#### 4.1.2 Connection Pooling Variation

```javascript
class ConnectionPoolVariation {
  constructor() {
    this.poolSize = 6; // Chrome default
    this.connectionLimits = {
      'http': 6,
      'https': 6,
      'proxy': 4
    };
  }

  // Get connection pool size
  getPoolSize(protocol = 'https') {
    // Chrome: 6 connections per domain
    // But varies ±1-2 based on profile
    
    const base = this.connectionLimits[protocol] || 6;
    const variance = Math.floor((Math.random() - 0.5) * 2);
    
    return Math.max(2, base + variance);
  }
}
```

#### 4.1.3 Port Variation

```javascript
class PortVariation {
  constructor() {
    this.usedPorts = new Set();
    this.portRange = { min: 49152, max: 65535 }; // Ephemeral range
  }

  // Get random ephemeral port
  getEphemeralPort() {
    // Browsers use ephemeral ports (49152-65535)
    // Vary within realistic range
    
    const port = this.portRange.min + Math.floor(Math.random() * (this.portRange.max - this.portRange.min));
    this.usedPorts.add(port);
    
    return port;
  }
}
```

### 4.2 Test Coverage (10 tests)

- [ ] DNS queries cached realistically
- [ ] Query delays vary 5-50ms
- [ ] Connection pool size varies ±1-2 from baseline
- [ ] Protocol-specific pool limits enforced
- [ ] Ephemeral port selection is random
- [ ] Port range stays within valid bounds
- [ ] Port reuse avoided (until TTL expires)
- [ ] DNS pattern obfuscation produces variations
- [ ] Connection pooling follows browser behavior
- [ ] Port allocation doesn't leak system info

---

## FEATURE 5: DETECTION SERVICE TESTING

**Duration:** 0.5 days | **Tests:** 10 | **LOC:** 100-150

### 5.1 Implementation Details

```javascript
class DetectionServiceTester {
  constructor() {
    this.services = {
      'perimeterx': { endpoint: 'https://perimeterx.test', signatures: [] },
      'datadome': { endpoint: 'https://datadome.test', signatures: [] },
      'recaptcha': { endpoint: 'https://recaptcha.test', signatures: [] },
      'cloudflare': { endpoint: 'https://cloudflare.test', signatures: [] },
      'distil': { endpoint: 'https://distil.test', signatures: [] }
    };
  }

  // Test evasion against detection service
  async testDetectionService(service, testData) {
    // Make request to service endpoint
    // Check if detected as bot
    // Log fingerprints for analysis
    
    const result = await this._sendTestRequest(service, testData);
    const effectiveness = this._analyzeResult(result);
    
    return {
      service,
      detected: result.detected,
      confidence: result.confidence,
      effectiveness: effectiveness,
      improvements: this._suggestImprovements(result)
    };
  }

  _analyzeResult(result) {
    // Higher score = better evasion
    // 0-100 scale
    
    return 100 - (result.confidence || 0);
  }

  _suggestImprovements(result) {
    // Return list of evasion techniques to improve
    return [];
  }
}
```

### 5.2 Test Coverage (10 tests)

- [ ] All 5 detection services tested
- [ ] Evasion effectiveness >85% per service
- [ ] Detection triggers logged
- [ ] Fingerprint variations identified
- [ ] Improvement suggestions generated
- [ ] Real-world site testing (github.com, google.com)
- [ ] Behavioral detection avoided
- [ ] Timing patterns normal
- [ ] Network characteristics spoofed
- [ ] Session coherence maintained

---

## TESTING STRATEGY

### Test Execution Plan

**Phase 1: Unit Tests (Days 1-2)**
```
TLS Fingerprinting Evasion: 25 tests
  - Cipher rotation (8)
  - TLS version spoofing (6)
  - Extension ordering (6)
  - Certificate validation (5)

HTTP/2 Evasion: 20 tests
  - Header ordering (5)
  - Priority manipulation (5)
  - Window obfuscation (5)
  - Settings spoofing (5)

Timing Prevention: 15 tests
  - Request timing (8)
  - Response delay (4)
  - Connection reuse (3)

Network Evasion: 10 tests
  - DNS obfuscation (3)
  - Connection pooling (4)
  - Port variation (3)
```

**Phase 2: Integration Tests (Day 2-3)**
- TLS + HTTP/2 coherence (15 tests)
- Timing + Network coordination (10 tests)
- All layers together (10 tests)

**Phase 3: Detection Service Testing (Day 3-4)**
- PerimeterX testing (2 tests)
- DataDome testing (2 tests)
- reCAPTCHA testing (2 tests)
- Cloudflare testing (2 tests)
- Distil Networks testing (2 tests)

**Total: 80+ tests**

### Test Quality Metrics

| Metric | Target | Acceptance |
|--------|--------|-----------|
| Pass Rate | >95% | >92% |
| Code Coverage | >85% | >80% |
| Performance Impact | <5% | <10% |
| False Positive Rate | <1% | <2% |
| Detection Evasion | >90% | >85% |

---

## SUCCESS CRITERIA

### Functionality

- [ ] All 80+ tests passing
- [ ] TLS fingerprinting evasion >90%
- [ ] HTTP/2 evasion >85%
- [ ] Timing patterns statistically indistinguishable
- [ ] Detection service bypass >90%

### Performance

- [ ] Latency impact <5% (<2.1ms P99)
- [ ] Memory overhead <10MB
- [ ] CPU impact <3% under load
- [ ] Throughput maintained (>400 msg/sec)

### Integration

- [ ] Zero breaking changes
- [ ] Compatible with existing evasion layers
- [ ] Coherence score >85%
- [ ] WebSocket commands functional

### Documentation

- [ ] API documentation complete
- [ ] Implementation guide written
- [ ] Configuration examples provided
- [ ] Effectiveness metrics documented

---

## DELIVERABLES

### Code Files (1200-1600 LOC)

```
src/evasion/
  ├── tls-cipher-rotation.js (120 LOC)
  ├── tls-extension-ordering.js (100 LOC)
  ├── certificate-validation-evasion.js (80 LOC)
  ├── alpn-protocol-selection.js (60 LOC)
  ├── http2-header-ordering.js (100 LOC)
  ├── http2-priority-manipulation.js (120 LOC)
  ├── http2-window-obfuscation.js (140 LOC)
  ├── http2-settings-evasion.js (100 LOC)
  ├── timing-attack-prevention.js (150 LOC)
  ├── request-timing-randomization.js (80 LOC)
  ├── response-delay-injection.js (60 LOC)
  ├── connection-reuse-patterns.js (80 LOC)
  ├── dns-pattern-obfuscation.js (60 LOC)
  ├── connection-pool-variation.js (50 LOC)
  ├── port-variation.js (40 LOC)
  └── detection-service-tester.js (150 LOC)

websocket/
  └── server.js (update with 6 new commands, ~50 LOC)

tests/
  ├── evasion/evasion-tls-extended.test.js (200 LOC)
  ├── evasion/evasion-http2-extended.test.js (180 LOC)
  ├── evasion/evasion-timing.test.js (150 LOC)
  ├── evasion/evasion-network.test.js (100 LOC)
  └── evasion/detection-service-integration.test.js (150 LOC)
```

### WebSocket Commands (6 new)

```javascript
case 'enable_tls_evasion':
  // Activate TLS fingerprinting evasion
  // Returns: { tls_evasion: 'enabled', cipher_rotation: 'realistic' }

case 'enable_http2_evasion':
  // Activate HTTP/2 evasion
  // Returns: { http2_evasion: 'enabled', header_ordering: 'realistic' }

case 'enable_timing_evasion':
  // Activate timing attack prevention
  // Returns: { timing_evasion: 'enabled', request_delay_range: [10, 150] }

case 'set_evasion_profile':
  // Set comprehensive evasion configuration
  // Param: { profile: 'chrome131-windows', aggressiveness: 'realistic' }
  // Returns: { profile: 'chrome131-windows', layers_enabled: 5 }

case 'test_evasion_effectiveness':
  // Test evasion against detection service
  // Param: { service: 'perimeterx', test_count: 10 }
  // Returns: { effectiveness: 88.5, detected: 1, passed: 9 }

case 'get_evasion_status':
  // Get current evasion status
  // Returns: { tls: 'enabled', http2: 'enabled', timing: 'enabled', ... }
```

### Documentation

```
docs/
  ├── EVASION-VECTORS.md (comprehensive guide)
  ├── TLS-EVASION.md (technical details)
  ├── HTTP2-EVASION.md (technical details)
  ├── TIMING-EVASION.md (technical details)
  └── DETECTION-SERVICE-TESTING.md (test methodology)
```

---

## EXECUTION TIMELINE

### Day 1 (6 hours)
- [ ] TLS fingerprinting evasion (cipher, version, extensions)
- [ ] Unit tests for TLS (25 tests)
- [ ] Initial integration with coordinator

### Day 2 (4 hours)
- [ ] HTTP/2 evasion complete (headers, priority, window, settings)
- [ ] Unit tests for HTTP/2 (20 tests)
- [ ] Integration tests (15 tests)

### Day 3 (4 hours)
- [ ] Timing + Network evasion complete
- [ ] Unit tests complete (25 tests)
- [ ] Detection service testing (10 tests)
- [ ] WebSocket commands implemented

### Day 4 (2 hours)
- [ ] Final validation and documentation
- [ ] All 80+ tests passing
- [ ] Performance baseline validated

---

## RISK MITIGATION

### Risk 1: Evasion Too Aggressive Breaks Sites
**Mitigation:** Start with conservative profiles, test on real sites, fallback mechanisms

### Risk 2: Timing Patterns Reveal Automation
**Mitigation:** Validate against timing analysis tools, add stochastic variance

### Risk 3: TLS Configuration Incompatible
**Mitigation:** Test with multiple servers, validate against RFC, compatibility matrix

### Risk 4: Performance Regression
**Mitigation:** Profile each module, optimize critical paths, <5% target

---

## NOTES FOR IMPLEMENTATION AGENT

1. **Reuse Existing Patterns:**
   - Follow existing evasion module structure
   - Use same test patterns as `advanced-evasion-comprehensive.test.js`
   - Integrate into `multi-layer-coordinator.js` layer system

2. **Critical Dependencies:**
   - TLS layer must be stable before HTTP/2
   - Timing evasion requires accurate request/response tracking
   - Detection service testing needs real endpoints

3. **Testing Strategy:**
   - Unit tests first (each module independent)
   - Integration tests next (layers working together)
   - Real-world validation (github.com, google.com, etc.)

4. **Documentation:**
   - Each module needs JSDoc comments
   - Effectiveness metrics must be measurable
   - Configuration options must be clear

5. **Performance Considerations:**
   - Timing delays must be added correctly (not synchronous blocking)
   - Cipher/extension manipulation should be cached per-session
   - Detection testing shouldn't impact main execution path

---

## APPROVAL & HANDOFF

**Status:** ✅ READY FOR IMPLEMENTATION

**Handoff Checklist:**
- [ ] All 80+ tests passing
- [ ] Performance impact <5% validated
- [ ] WebSocket commands working
- [ ] Documentation complete
- [ ] Real-world testing successful
- [ ] Zero breaking changes
- [ ] Ready for v12.7.0 release

---

*End of v12.7.0 Extended Evasion Vectors Feature Plan*
